const headerAnimation = require('./presets/headerAnimation');
const screenAnimation = require('./presets/screenAnimation');
const numberAnimation = require('./presets/numberAnimation');

module.exports = {
    headerAnimation,
    screenAnimation,
    numberAnimation
};