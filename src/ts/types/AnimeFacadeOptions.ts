export type AnimeFacadeOptions = {
    preset?: object,
    custom?: object,
    easing?: string,
    duration?: number | object | Function,
    delay?: number | object | Function,
    direction?: string,
    autoplay?: boolean,
    rootMargin?: string
}